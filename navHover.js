'use strict'
    function colorCh(obj) {
            if (obj.style.color == 'black') {
                obj.style.color = 'whitesmoke';
            } else {
                obj.style.color = 'black';
            }
        }
